# Need stress testing for performance review such as streaming billions of points with large grids

# Multi-threaded cpu

# Need to test real life data sets. Lidar LAS/LAZ datasets

# Need to fix being able to run in a container

# Packaging for easier test?

# GPU doesn't seem to be in-use with "gpu enabled" and is slower
